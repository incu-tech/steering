node_modules
dist
*.log
.DS_Store

# internal design docs — kept locally, never committed
docs/
design.md
LIMITATIONS.md

# ways CLI cache only — .ways/state.json MUST stay tracked (resume-by-branch)
.ways/cache/
